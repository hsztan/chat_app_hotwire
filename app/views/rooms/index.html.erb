<%= turbo_frame_tag 'rooms-controller' do %>
  <main class="flex w-full h-screen bg-[#181D31]">
    <%= render 'shared/side_bar' %>

    <section class="flex flex-col items-center justify-center h-screen gap-5">
      <%= link_to "New room", new_room_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
    </section>

  </main>
<% end %>
